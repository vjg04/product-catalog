apiVersion: v1
kind: Service

metadata:
  name: product-api-service
  namespace: product-catalog

spec:
  selector:
    app: product-api

  ports:
  - port: 80
    targetPort: 8000

  type: LoadBalancer